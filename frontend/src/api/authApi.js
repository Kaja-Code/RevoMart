// Example placeholder for future Node/Express/Mongo calls using fetch/axios
export async function login(email, password) {
  // return await axios.post('/auth/login', { email, password });
  return { ok: true };
}
